services:
  - type: web
    name: dss-maintenance-web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python download_output.py -p outputnotebook
    startCommand: gunicorn web.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
